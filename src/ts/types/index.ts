/**
 * Barrel export for kist's shared type aliases. Currently just
 * {@link ActionOptionsType}, the loosely-typed options bag every
 * {@link Action.execute} implementation receives.
 */

export { ActionOptionsType } from "./ActionOptionsType.js";
