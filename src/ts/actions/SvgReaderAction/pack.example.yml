# =============================================================================
# Pack Configuration for Reading SVG Files
# =============================================================================

stages:
  - name: Read SVG File
    steps:
      - name: Load SVG Content
        action: SvgReaderAction
        options:
          filePath: "src/icons/logo.svg"
