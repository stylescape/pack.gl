# Require exact versions in package-lock.json
save-exact=true

# Set engine-strict to prevent installing on incompatible Node versions
engine-strict=true

# Reduce noise during install
loglevel=warn

# Use legacy peer dependencies resolution for compatibility
legacy-peer-deps=false
